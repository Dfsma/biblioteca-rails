<%= form_with(model: idioma, local: true) do |form| %>
  <% if idioma.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(idioma.errors.count, "error") %> prohibited this idioma from being saved:</h2>

      <ul>
        <% idioma.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
